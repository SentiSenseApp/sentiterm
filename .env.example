# SentiSense API Key — sign up at https://sentisense.ai
SENTISENSE_API_KEY=

# AI Provider API Key (optional — terminal works without it)
ANTHROPIC_API_KEY=

# Use mock data instead of live API (set to "true" for demo mode)
SENTISENSE_USE_MOCKS=true
